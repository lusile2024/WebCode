@page "/setup"
@namespace WebCodeCli.Pages
@using WebCodeCli.Domain.Domain.Service
@using Microsoft.AspNetCore.Components
@using Microsoft.AspNetCore.Components.Routing
@using WebCodeCli.Components

<PageTitle>@T("setup.pageTitle")</PageTitle>

<div class="min-h-screen bg-gradient-to-br from-gray-50 via-gray-100 to-gray-200 py-4 md:py-8 px-3 md:px-4">
    <div class="max-w-4xl mx-auto">
        <div class="flex justify-end mb-4">
            <LanguageSwitcher OnLanguageChanged="HandleLanguageChanged" />
        </div>
        <!-- 头部 Logo -->
        <div class="text-center mb-6 md:mb-8">
            <div class="inline-flex items-center justify-center mb-3 md:mb-4">
                <img src="/images/logo.png" alt="WebCode Logo" class="w-16 h-16 md:w-20 md:h-20 object-contain" />
            </div>
            <h1 class="text-2xl md:text-3xl font-bold text-gray-800 mb-2">@T("setup.title")</h1>
            <p class="text-sm md:text-base text-gray-600">@T("setup.subtitle")</p>
        </div>

        <!-- 步骤指示器 -->
        <div class="flex items-center justify-center mb-6 md:mb-8 overflow-x-auto pb-2">
            @{
                var totalSteps = GetTotalSteps();
                var displayStep = GetDisplayStep();
            }
            @for (int i = 1; i <= totalSteps; i++)
            {
                var step = i;
                <div class="flex items-center flex-shrink-0">
                    <div class="@GetStepClass(step)">
                        @if (displayStep > step)
                        {
                            <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                        }
                        else
                        {
                            @step
                        }
                    </div>
                    <span class="ml-1.5 md:ml-2 text-xs md:text-sm font-medium @(step <= displayStep ? "text-gray-800" : "text-gray-400") hidden sm:inline">
                        @GetStepTitle(step)
                    </span>
                    @if (step < totalSteps)
                    {
                        <div class="w-6 md:w-12 h-0.5 mx-2 md:mx-4 @(step < displayStep ? "bg-gray-800" : "bg-gray-300")"></div>
                    }
                </div>
            }
        </div>

        <!-- 主内容卡片 -->
        <div class="bg-white rounded-2xl shadow-2xl border border-gray-200 overflow-hidden">
            @if (_isLoading)
            {
                <div class="flex items-center justify-center py-20">
                    <div class="text-center">
                        <div class="animate-spin h-12 w-12 border-4 border-gray-700 rounded-full border-t-transparent mx-auto mb-4"></div>
                        <p class="text-gray-600">@T("setup.loading")</p>
                    </div>
                </div>
            }
            else if (_isCompleted)
            {
                <!-- 完成页面 -->
                <div class="p-6 md:p-12 text-center">
                    <div class="w-16 h-16 md:w-20 md:h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4 md:mb-6">
                        <svg class="w-8 h-8 md:w-10 md:h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                    </div>
                    <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-3 md:mb-4">@T("setup.completedTitle")</h2>
                    <p class="text-sm md:text-base text-gray-600 mb-6 md:mb-8">@T("setup.completedDesc")</p>
                    <button @onclick="GoToHome" class="btn-primary px-6 md:px-8 py-2.5 md:py-3 text-base md:text-lg">
                        @T("setup.start")
                    </button>
                </div>
            }
            else
            {
                <!-- 步骤内容 -->
                @switch (_currentStep)
                {
                    case 1:
                        @RenderStep1()
                        break;
                    case 2:
                        @RenderStepAssistantSelection()
                        break;
                    case 3:
                        @RenderCurrentAssistantConfig()
                        break;
                }
            }
        </div>

        <!-- 导入本地配置弹窗 -->
        @if (_showImportPrompt && _configsToImport.Any())
        {
            <div class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4">
                <div class="bg-white rounded-2xl shadow-2xl max-w-2xl w-full max-h-[80vh] overflow-hidden">
                    <div class="p-4 md:p-6 border-b border-gray-200">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 bg-blue-100 rounded-xl flex items-center justify-center">
                                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
                                </svg>
                            </div>
                            <div>
                                <h3 class="text-lg font-bold text-gray-800">@T("setup.importConfigTitle")</h3>
                                <p class="text-sm text-gray-600">@T("setup.importConfigDesc")</p>
                            </div>
                        </div>
                    </div>
                    <div class="p-4 md:p-6 overflow-y-auto max-h-[50vh]">
                        @foreach (var config in _configsToImport)
                        {
                            <div class="mb-4 p-3 md:p-4 bg-gray-50 rounded-xl border border-gray-200">
                                <div class="flex items-center gap-2 mb-3">
                                    <div class="@GetConfigIconClass(config.ToolId) w-8 h-8 rounded-lg flex items-center justify-center">
                                        @GetConfigIcon(config.ToolId)
                                    </div>
                                    <div>
                                        <h4 class="font-semibold text-gray-800">@config.ToolName</h4>
                                        <p class="text-xs text-gray-500">
                                            @if (config.FromConfigFile)
                                            {
                                                <span>@T("setup.fromConfigFile"): @config.ConfigFilePath</span>
                                            }
                                            else if (config.FromSystemEnv)
                                            {
                                                <span>@T("setup.fromSystemEnv")</span>
                                            }
                                        </p>
                                    </div>
                                    @if (config.HasValidApiKey)
                                    {
                                        <span class="ml-auto px-2 py-1 bg-green-100 text-green-700 text-xs font-semibold rounded-full">@T("setup.hasApiKey")</span>
                                    }
                                </div>
                                <div class="space-y-2">
                                    @foreach (var envVar in config.EnvironmentVariables)
                                    {
                                        <div class="flex items-center gap-2 text-sm">
                                            <span class="font-mono text-gray-700 bg-white px-2 py-1 rounded border border-gray-200 text-xs">@envVar.Key</span>
                                            <span class="text-gray-400">=</span>
                                            <span class="text-gray-600 truncate flex-1">@(envVar.Value.Length > 30 ? envVar.Value.Substring(0, 30) + "..." : envVar.Value)</span>
                                        </div>
                                    }
                                </div>
                            </div>
                        }
                    </div>
                    <div class="p-4 md:p-6 border-t border-gray-200 flex flex-col sm:flex-row gap-3">
                        <button @onclick="SkipImportConfigs" class="btn-default flex-1 sm:flex-none">
                            @T("setup.skipImport")
                        </button>
                        <button @onclick="ImportDetectedConfigs" class="btn-primary flex-1 sm:flex-none">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
                            </svg>
                            @T("setup.importSelected")
                        </button>
                    </div>
                </div>
            </div>
        }

        <!-- 错误提示 -->
        @if (!string.IsNullOrEmpty(_errorMessage))
        {
            <div class="mt-6 bg-red-50 border-l-4 border-red-500 text-red-700 px-4 py-3 rounded-lg">
                <div class="flex items-start gap-2">
                    <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span>@_errorMessage</span>
                </div>
            </div>
        }

        <!-- 页脚 -->
        <div class="mt-8 text-center text-sm text-gray-500">
            <p>@T("setup.footer", ("year", DateTime.Now.Year.ToString()))</p>
        </div>
    </div>
</div>

@code {
    /// <summary>
    /// 第一步：账户设置
    /// </summary>
    private RenderFragment RenderStep1() => __builder =>
    {
        <div class="p-4 md:p-8">
            <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center gap-2">
                <svg class="w-5 h-5 md:w-6 md:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
                @T("setup.step1Header")
            </h2>

            <div class="space-y-4 md:space-y-6">
                <!-- 启用认证 -->
                <div class="flex items-center justify-between p-3 md:p-4 bg-gray-50 rounded-xl border border-gray-200 gap-3">
                    <div class="flex-1 min-w-0">
                        <h3 class="font-semibold text-gray-800 text-sm md:text-base">@T("setup.enableAuthTitle")</h3>
                        <p class="text-xs md:text-sm text-gray-600">@T("setup.enableAuthDesc")</p>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer flex-shrink-0">
                        <input type="checkbox" @bind="_config.EnableAuth" class="sr-only peer">
                        <div class="w-11 h-6 bg-gray-300 peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-gray-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-gray-800"></div>
                    </label>
                </div>

                @if (_config.EnableAuth)
                {
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">@T("setup.adminUsernameLabel")</label>
                            <input type="text" 
                                   @bind="_config.AdminUsername"
                                   class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent"
                                   placeholder="@T("setup.adminUsernamePlaceholder")" />
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">@T("setup.adminPasswordLabel")</label>
                            <input type="password" 
                                   @bind="_config.AdminPassword"
                                   class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent"
                                   placeholder="@T("setup.adminPasswordPlaceholder")" />
                        </div>
                    </div>
                }

                <!-- 工作区目录 -->
                <div>
                    <label class="block text-xs md:text-sm font-medium text-gray-700 mb-2">
                        @T("setup.workspaceRootLabel") 
                        <span class="text-gray-400 font-normal">@T("setup.workspaceRootHint")</span>
                    </label>
                    <input type="text" 
                           @bind="_config.WorkspaceRoot"
                           class="w-full px-3 md:px-4 py-2.5 md:py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent text-sm md:text-base"
                           placeholder="@_defaultWorkspaceRoot" />
                    <p class="mt-2 text-xs md:text-sm text-gray-500">
                        @T("setup.workspaceRootDesc", ("path", "/app/workspaces"))
                    </p>
                </div>
            </div>

            <!-- 底部按钮 -->
            <div class="flex justify-end mt-6 md:mt-8 pt-4 md:pt-6 border-t border-gray-200">
                <button @onclick="NextStep" class="btn-primary">
                    @T("common.next")
                    <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </button>
            </div>
        </div>
    };

    /// <summary>
    /// 第二步：选择要配置的助手
    /// </summary>
    private RenderFragment RenderStepAssistantSelection() => __builder =>
    {
        <div class="p-4 md:p-8">
            <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center gap-2">
                <svg class="w-5 h-5 md:w-6 md:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
                </svg>
                @T("setup.selectAssistantsHeader")
            </h2>

            <div class="bg-blue-50 border border-blue-200 rounded-xl p-3 md:p-4 mb-4 md:mb-6">
                <div class="flex">
                    <svg class="w-4 h-4 md:w-5 md:h-5 text-blue-600 mt-0.5 mr-2 md:mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                    </svg>
                    <div class="text-xs md:text-sm text-blue-800">
                        <p class="font-bold mb-1">@T("setup.selectAssistantsTip")</p>
                        <p>@T("setup.selectAssistantsDesc")</p>
                    </div>
                </div>
            </div>

            <div class="space-y-3 md:space-y-4">
                <!-- Claude Code -->
                <div class="@GetAssistantCardClass(_enableClaudeCode) p-3 md:p-5 rounded-xl border-2 cursor-pointer transition-all hover:shadow-md"
                     @onclick="() => _enableClaudeCode = !_enableClaudeCode">
                    <div class="flex items-center gap-3 md:gap-4">
                        <div class="flex-shrink-0">
                            <input type="checkbox" checked="@_enableClaudeCode" 
                                   @onclick:stopPropagation="true"
                                   @onchange="() => _enableClaudeCode = !_enableClaudeCode"
                                   class="w-4 h-4 md:w-5 md:h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500 cursor-pointer" />
                        </div>
                        <div class="flex-shrink-0 w-10 h-10 md:w-12 md:h-12 bg-orange-100 rounded-xl flex items-center justify-center">
                            <svg class="w-5 h-5 md:w-7 md:h-7 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                        </div>
                        <div class="flex-1 min-w-0">
                            <h3 class="font-bold text-gray-800 text-base md:text-lg">Claude Code</h3>
                            <p class="text-xs md:text-sm text-gray-600 truncate">@T("setup.claudeCodeDesc")</p>
                        </div>
                        @if (_enableClaudeCode)
                        {
                            <div class="flex-shrink-0 hidden sm:block">
                                <span class="px-2 md:px-3 py-1 bg-orange-100 text-orange-700 text-xs font-semibold rounded-full">@T("setup.selected")</span>
                            </div>
                        }
                    </div>
                </div>

                <!-- Codex -->
                <div class="@GetAssistantCardClass(_enableCodex) p-3 md:p-5 rounded-xl border-2 cursor-pointer transition-all hover:shadow-md"
                     @onclick="() => _enableCodex = !_enableCodex">
                    <div class="flex items-center gap-3 md:gap-4">
                        <div class="flex-shrink-0">
                            <input type="checkbox" checked="@_enableCodex" 
                                   @onclick:stopPropagation="true"
                                   @onchange="() => _enableCodex = !_enableCodex"
                                   class="w-4 h-4 md:w-5 md:h-5 text-green-600 border-gray-300 rounded focus:ring-green-500 cursor-pointer" />
                        </div>
                        <div class="flex-shrink-0 w-10 h-10 md:w-12 md:h-12 bg-green-100 rounded-xl flex items-center justify-center">
                            <svg class="w-5 h-5 md:w-7 md:h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                            </svg>
                        </div>
                        <div class="flex-1 min-w-0">
                            <h3 class="font-bold text-gray-800 text-base md:text-lg">Codex</h3>
                            <p class="text-xs md:text-sm text-gray-600 truncate">@T("setup.codexDesc")</p>
                        </div>
                        @if (_enableCodex)
                        {
                            <div class="flex-shrink-0 hidden sm:block">
                                <span class="px-2 md:px-3 py-1 bg-green-100 text-green-700 text-xs font-semibold rounded-full">@T("setup.selected")</span>
                            </div>
                        }
                    </div>
                </div>

                <!-- OpenCode -->
                <div class="@GetAssistantCardClass(_enableOpenCode) p-3 md:p-5 rounded-xl border-2 cursor-pointer transition-all hover:shadow-md"
                     @onclick="() => _enableOpenCode = !_enableOpenCode">
                    <div class="flex items-center gap-3 md:gap-4">
                        <div class="flex-shrink-0">
                            <input type="checkbox" checked="@_enableOpenCode" 
                                   @onclick:stopPropagation="true"
                                   @onchange="() => _enableOpenCode = !_enableOpenCode"
                                   class="w-4 h-4 md:w-5 md:h-5 text-purple-600 border-gray-300 rounded focus:ring-purple-500 cursor-pointer" />
                        </div>
                        <div class="flex-shrink-0 w-10 h-10 md:w-12 md:h-12 bg-purple-100 rounded-xl flex items-center justify-center">
                            <svg class="w-5 h-5 md:w-7 md:h-7 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                            </svg>
                        </div>
                        <div class="flex-1 min-w-0">
                            <h3 class="font-bold text-gray-800 text-base md:text-lg">OpenCode</h3>
                            <p class="text-xs md:text-sm text-gray-600 truncate">@T("setup.openCodeDesc")</p>
                        </div>
                        @if (_enableOpenCode)
                        {
                            <div class="flex-shrink-0 hidden sm:block">
                                <span class="px-2 md:px-3 py-1 bg-purple-100 text-purple-700 text-xs font-semibold rounded-full">@T("setup.selected")</span>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- 底部按钮 -->
            <div class="flex flex-col sm:flex-row justify-between gap-3 mt-6 md:mt-8 pt-4 md:pt-6 border-t border-gray-200">
                <button @onclick="PrevStep" class="btn-default order-2 sm:order-1">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                    @T("common.previous")
                </button>
                <div class="flex gap-3 order-1 sm:order-2">
                    @if (!GetEnabledAssistants().Any())
                    {
                        <button @onclick="CompleteSetup" class="btn-primary flex-1 sm:flex-none">
                            @T("setup.skipAndComplete")
                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                        </button>
                    }
                    else
                    {
                        <button @onclick="StartAssistantConfig" class="btn-primary flex-1 sm:flex-none">
                            <span class="truncate">@T("setup.configureAssistants") (@GetEnabledAssistants().Count)</span>
                            <svg class="w-4 h-4 ml-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                        </button>
                    }
                </div>
            </div>
        </div>
    };

    /// <summary>
    /// 第三步：配置当前选中的助手
    /// </summary>
    private RenderFragment RenderCurrentAssistantConfig() => __builder =>
    {
        var enabledAssistants = GetEnabledAssistants();
        if (_currentAssistantIndex >= enabledAssistants.Count)
        {
            return;
        }

        var currentAssistant = enabledAssistants[_currentAssistantIndex];
        var isLast = _currentAssistantIndex == enabledAssistants.Count - 1;

        <div class="p-4 md:p-8">
            @switch (currentAssistant)
            {
                case "claude-code":
                    @RenderClaudeCodeConfig(isLast)
                    break;
                case "codex":
                    @RenderCodexConfig(isLast)
                    break;
                case "opencode":
                    @RenderOpenCodeConfig(isLast)
                    break;
            }
        </div>
    };

    /// <summary>
    /// Claude Code 配置
    /// </summary>
    private RenderFragment RenderClaudeCodeConfig(bool isLast) => __builder =>
    {
        <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center gap-2 flex-wrap">
            <div class="w-7 h-7 md:w-8 md:h-8 bg-orange-100 rounded-lg flex items-center justify-center">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
            </div>
            @T("setup.step2Header")
            <span class="text-xs md:text-sm font-normal text-gray-500">(@(_currentAssistantIndex + 1)/@GetEnabledAssistants().Count)</span>
        </h2>

        <div class="bg-blue-50 border border-blue-200 rounded-xl p-3 md:p-4 mb-4 md:mb-6">
            <div class="flex">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-blue-600 mt-0.5 mr-2 md:mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                </svg>
                <div class="text-xs md:text-sm text-blue-800">
                    <p class="font-bold mb-1">@T("setup.configTipTitle")</p>
                    <p>@T("setup.claudeConfigDesc")</p>
                </div>
            </div>
        </div>

        <div class="space-y-3 md:space-y-4">
            @foreach (var envVar in _claudeEnvVars)
            {
                <div class="bg-gray-50 rounded-xl p-3 md:p-4 border border-gray-200">
                    <div class="flex flex-col md:flex-row md:items-end gap-2 md:gap-4">
                        <div class="flex-1 md:w-1/3">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarNameLabel")</label>
                            <input type="text" 
                                   @bind="envVar.Key"
                                   class="w-full px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                   placeholder="@T("setup.envVarNamePlaceholderClaude")" />
                        </div>
                        <div class="flex-1 md:flex-[2]">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarValueLabel")</label>
                            <div class="flex gap-2">
                                <input type="text" 
                                       @bind="envVar.Value"
                                       class="flex-1 px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                       placeholder="@GetEnvVarPlaceholder(envVar.Key)" />
                                <button @onclick="() => RemoveClaudeEnvVar(envVar)" 
                                        class="p-2 md:p-2.5 text-red-600 hover:bg-red-50 rounded-xl transition-all flex-shrink-0">
                                    <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                    </svg>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <button @onclick="AddClaudeEnvVar" 
                    class="w-full py-2.5 md:py-3 border-2 border-dashed border-gray-400 rounded-xl hover:border-gray-600 hover:bg-gray-50 transition-all flex items-center justify-center gap-2 text-gray-700 text-sm">
                <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                @T("setup.addEnvVar")
            </button>
        </div>

        @RenderAssistantConfigButtons(isLast)
    };

    /// <summary>
    /// Codex 配置
    /// </summary>
    private RenderFragment RenderCodexConfig(bool isLast) => __builder =>
    {
        <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center gap-2 flex-wrap">
            <div class="w-7 h-7 md:w-8 md:h-8 bg-green-100 rounded-lg flex items-center justify-center">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                </svg>
            </div>
            @T("setup.step3Header")
            <span class="text-xs md:text-sm font-normal text-gray-500">(@(_currentAssistantIndex + 1)/@GetEnabledAssistants().Count)</span>
        </h2>

        <div class="bg-green-50 border border-green-200 rounded-xl p-3 md:p-4 mb-4 md:mb-6">
            <div class="flex">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-green-600 mt-0.5 mr-2 md:mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                </svg>
                <div class="text-xs md:text-sm text-green-800">
                    <p class="font-bold mb-1">@T("setup.configTipTitle")</p>
                    <p>@T("setup.codexConfigDesc")</p>
                    <a class="text-green-700 underline" href="https://developers.openai.com/codex/config-sample/" target="_blank" rel="noopener noreferrer">
                        @T("setup.codexDocsLink")
                    </a>
                </div>
            </div>
        </div>

        <div class="space-y-3 md:space-y-4">
            @foreach (var envVar in _codexEnvVars)
            {
                <div class="bg-gray-50 rounded-xl p-3 md:p-4 border border-gray-200">
                    <div class="flex flex-col md:flex-row md:items-end gap-2 md:gap-4">
                        <div class="flex-1 md:w-1/3">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarNameLabel")</label>
                            <input type="text" 
                                   @bind="envVar.Key"
                                   class="w-full px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                   placeholder="@T("setup.envVarNamePlaceholderCodex")" />
                        </div>
                        <div class="flex-1 md:flex-[2]">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarValueLabel")</label>
                            <div class="flex gap-2">
                                <input type="text" 
                                       @bind="envVar.Value"
                                       class="flex-1 px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                       placeholder="@GetEnvVarPlaceholder(envVar.Key)" />
                                <button @onclick="() => RemoveCodexEnvVar(envVar)" 
                                        class="p-2 md:p-2.5 text-red-600 hover:bg-red-50 rounded-xl transition-all flex-shrink-0">
                                    <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                    </svg>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <button @onclick="AddCodexEnvVar" 
                    class="w-full py-2.5 md:py-3 border-2 border-dashed border-gray-400 rounded-xl hover:border-gray-600 hover:bg-gray-50 transition-all flex items-center justify-center gap-2 text-gray-700 text-sm">
                <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                @T("setup.addEnvVar")
            </button>
        </div>

        @RenderAssistantConfigButtons(isLast)
    };

    /// <summary>
    /// OpenCode 配置
    /// </summary>
    private RenderFragment RenderOpenCodeConfig(bool isLast) => __builder =>
    {
        <h2 class="text-lg md:text-xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center gap-2 flex-wrap">
            <div class="w-7 h-7 md:w-8 md:h-8 bg-purple-100 rounded-lg flex items-center justify-center">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
            </div>
            @T("setup.step4Header")
            <span class="text-xs md:text-sm font-normal text-gray-500">(@(_currentAssistantIndex + 1)/@GetEnabledAssistants().Count)</span>
        </h2>

        <div class="bg-purple-50 border border-purple-200 rounded-xl p-3 md:p-4 mb-4 md:mb-6">
            <div class="flex">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-purple-600 mt-0.5 mr-2 md:mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                </svg>
                <div class="text-xs md:text-sm text-purple-800">
                    <p class="font-bold mb-1">@T("setup.configTipTitle")</p>
                    <p>@T("setup.opencodeConfigDesc")</p>
                    <a class="text-purple-700 underline" href="https://opencode.ai/docs/cli/#environment-variables" target="_blank" rel="noopener noreferrer">
                        @T("setup.opencodeDocsLink")
                    </a>
                </div>
            </div>
        </div>

        <div class="space-y-3 md:space-y-4">
            @foreach (var envVar in _openCodeEnvVars)
            {
                <div class="bg-gray-50 rounded-xl p-3 md:p-4 border border-gray-200">
                    <div class="flex flex-col md:flex-row md:items-end gap-2 md:gap-4">
                        <div class="flex-1 md:w-1/3">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarNameLabel")</label>
                            <input type="text" 
                                   @bind="envVar.Key"
                                   class="w-full px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                   placeholder="@T("setup.envVarNamePlaceholderOpenCode")" />
                        </div>
                        <div class="flex-1 md:flex-[2]">
                            <label class="block text-xs md:text-sm font-medium text-gray-700 mb-1 md:mb-2">@T("setup.envVarValueLabel")</label>
                            <div class="flex gap-2">
                                <input type="text" 
                                       @bind="envVar.Value"
                                       class="flex-1 px-3 md:px-4 py-2 md:py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-1 focus:ring-gray-500 text-sm"
                                       placeholder="@GetEnvVarPlaceholder(envVar.Key)" />
                                <button @onclick="() => RemoveOpenCodeEnvVar(envVar)" 
                                        class="p-2 md:p-2.5 text-red-600 hover:bg-red-50 rounded-xl transition-all flex-shrink-0">
                                    <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                    </svg>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <button @onclick="AddOpenCodeEnvVar" 
                    class="w-full py-2.5 md:py-3 border-2 border-dashed border-gray-400 rounded-xl hover:border-gray-600 hover:bg-gray-50 transition-all flex items-center justify-center gap-2 text-gray-700 text-sm">
                <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                @T("setup.addEnvVar")
            </button>
        </div>

        @RenderAssistantConfigButtons(isLast)
    };

    /// <summary>
    /// 助手配置页面的底部按钮
    /// </summary>
    private RenderFragment RenderAssistantConfigButtons(bool isLast) => __builder =>
    {
        <div class="flex flex-col sm:flex-row justify-between gap-3 mt-6 md:mt-8 pt-4 md:pt-6 border-t border-gray-200">
            <button @onclick="PrevAssistantConfig" class="btn-default order-2 sm:order-1">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
                @T("common.previous")
            </button>
            <div class="flex gap-3 order-1 sm:order-2">
                @if (isLast)
                {
                    <button @onclick="CompleteSetup" class="btn-primary flex-1 sm:flex-none">
                        @T("setup.completeSetup")
                        <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                    </button>
                }
                else
                {
                    <button @onclick="NextAssistantConfig" class="btn-primary flex-1 sm:flex-none">
                        @T("common.next")
                        <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </button>
                }
            </div>
        </div>
    };

    private string GetAssistantCardClass(bool isSelected)
    {
        return isSelected 
            ? "bg-gray-50 border-gray-800" 
            : "bg-white border-gray-200 hover:border-gray-400";
    }

    private void StartAssistantConfig()
    {
        _currentAssistantIndex = 0;
        _currentStep = 3;
        StateHasChanged();
    }

    private void NextAssistantConfig()
    {
        _currentAssistantIndex++;
        StateHasChanged();
    }

    private void PrevAssistantConfig()
    {
        if (_currentAssistantIndex > 0)
        {
            _currentAssistantIndex--;
        }
        else
        {
            _currentStep = 2;
        }
        StateHasChanged();
    }

    private string GetConfigIconClass(string toolId)
    {
        return toolId switch
        {
            "claude-code" => "bg-orange-100 text-orange-600",
            "codex" => "bg-green-100 text-green-600",
            "opencode" => "bg-purple-100 text-purple-600",
            _ => "bg-gray-100 text-gray-600"
        };
    }

    private RenderFragment GetConfigIcon(string toolId) => __builder =>
    {
        @switch (toolId)
        {
            case "claude-code":
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                break;
            case "codex":
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                </svg>
                break;
            case "opencode":
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
                break;
            default:
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
                break;
        }
    };
}
